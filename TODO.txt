=== IN PROGRESS ===

* Hantera att en tjänst uppslagen via tjänsteregistret "flyttas" till en ny endpoint. (RELATERAT TILL NÄSTA PUNKT)

* Tjänster som registreras i service-registret måste leasas
	- konsumenter: ska fortlöpande slå upp tjänster på bussen
	- service-bus-pu: ska ta bort tjänster från registret om leasen inte förnyats
	- server: ska regelbundet förnya leasen för de tjänster som tillhandahålls

=== BACKLOG ===

=== V 1.0 "Account Performance" ===

* LifeCycle-hantering av tjänster och andra astrix-komponenter.
	
* Städa bort alla TODO:s från kodbasen.

======

* Vid @AsterixServiceExport av att interface som inte implementeras ska kasta vettigt exception vid uppstart. Allra bäst vore en compiler-proccessor som veriferar detta.

* Det ska kastas ett exception vid uppstart om inte alla tjänster som specifieras i tjänste-deskriptorn faktiskt exporteras från aktuell applikation

* Inför gemensam klass för alla AsterixBeanFactory som håller rätt på vilken AsterixApiDescriptor som en given factory är skapad ur. 

* Hur ska AsterixBeanDefnitionRegistryPlugin/AsterixServiceRegistryComponent's hantera beroenden? De bönor som dessa registrerar kommer inte
  hanteras av Asterix. Hur ska då AsterixPlugins/ExternalDepenendencies/ServiceDependencies hanteras?
  
* Lägg till stöd så att man kan skapa asterix-bönor genom super-interface till de faktiska tjänsterna. 
	Ex: InstrumentService => ReferenceDataService. Om vi har en registrerad provider för InstrumentService så ska 
		man kunna fråga astrerix efter en böna av typen ReferenceDataService, och få tillbaka den InstrumentService som har registrerats. 

* Cleanup: Se över strukturen på service-suite-context. Ta bort oanvända abstraktioner.

* Se över de felemeddelanden som en användare får om något ExternalDependency saknas i app-contextet.

* Skapa enbart en instans av varje tjänst. Done?

* Versionshantering av tjänster - implementera - testa

* Optimerad trafik mot tjänsteregistret. Låt klienten fråga efter flera tjänster i samma slagning.

* Hantera att tjänsteregistret inte är tillgänglig när man skapar en tjänst. Done?

* Se över modulstruktur
	
* Övervakning av tjänsteregistret

=== DONE ===

* Stöd för multipla service-descriptorer för en enskild tjänst

* Ta bort top-level AsterixBeanDependencyResolver.

* Inför en modell för "omfattande" enhetstester av Asterix.

* Cleanup: Tydlig dependency-hantering för plugins. När de skapas ska alla dess dependencies injectas.
Det innebär att enbart AsterixContext får skapa plugin:er, och att AsterixContext injectar 
dependencies i samband med skapandet.

* Cleanup: Ta bort AsterixContextAware. Ingen plugin ska behöva tillgång till AsterixContext

* Tjänstekonsumenter ska kunna använda Asterix som factory-bean för att skapa tjänster på ett enkelt sätt.
	- Asterix-tjänster ska kunna autowiras in i de klasser som använder dessa.
	
* Cleanup: AsterixServiceProviderPlugin ska inte behöva skapa en AsterixServiceProvider utan AsterixServiceFactory[]

* Cleanup: AsterixServiceBus-pu ska använda AsterixRemotingFrameworkBean snarare än interna klasser för att skapa
en astrix-runtime.

* Inför en web-applikation som konsumerar tjänster för att säkerställa att icke-pu:ar hanteras korrekt.

* Se över namngivning på moduler/klasser
  - service-bus -> service-registry
  
* AsterixApiProviderPlugin.getProviderAnnotationType och AsterixApiProviderPlugin.consumes: En av dessa metoder ska bort

* När man skapar en asterix-böna ska första bindingsförsöket ske synkront

* Se över namngivning av de olika space som används från integrationstester.

* ServiceUnavailable

* Feltolerans - implementera

* Inrapportering av metrics från olika astrix-pluginer

* DirectComponent ska kunna användas utan att den behöver registreras programmatiskt.